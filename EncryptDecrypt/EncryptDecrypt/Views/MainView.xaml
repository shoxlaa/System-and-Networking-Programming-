<Window x:Class="EncryptDecrypt.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:v="clr-namespace:EncryptDecrypt.Views"
        xmlns:vm="clr-namespace:EncryptDecrypt.ViewModels"
        xmlns:local="clr-namespace:EncryptDecrypt.Views"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="400">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:ModelListViewModel}">
            <v:ModelListView />
             </DataTemplate> 
    </Window.Resources>
    <Grid>
        <ContentPresenter Content="{Binding CurrentViewModel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></ContentPresenter>
    </Grid>
</Window>
